nvitk.db.local_nifti_assets
===========================

.. py:module:: nvitk.db.local_nifti_assets

.. autoapi-nested-parse::

   Register reorganized local NIfTI files (``subject/TOF``, ``subject/4DFlow``) into ``assets``.

   Expected layout (same as QVT+/eICAB pipelines)::

       {nifti_root}/{subject_uid}/TOF/*.nii[.gz]
       {nifti_root}/{subject_uid}/4DFlow/**/*.nii[.gz]

   Under ``4DFlow/``, supported patterns for ``asset_slot`` (see :func:`_classify_4dflow_path`) include:

   - Directional encodings: ``{subject}/4DFlow/AP|FH|RL/<name>_m.nii`` → ``flow_ap_m``, ``flow_fh_ph``, etc.
   - Root derivatives: ``Angiography_3D.nii`` / ``Angiography_4D.nii`` → ``flow_angiography_3d`` / ``flow_angiography_4d``,
     and the same for ``ComplexDifference_*`` and ``VelocityMagnitude_*``.

   TOF files use ``asset_slot`` ``tof`` when there is a single NIfTI per subject, else ``tof_<stem>``.

   Do not store secrets in the dataset; this module only records file paths.

   See also :mod:`nvitk.db.xnat_config` for XNAT credentials.



Attributes
----------

.. autoapisummary::

   nvitk.db.local_nifti_assets.click
   nvitk.db.local_nifti_assets._click_command
   nvitk.db.local_nifti_assets._click_option


Functions
---------

.. autoapisummary::

   nvitk.db.local_nifti_assets._is_nifti_path
   nvitk.db.local_nifti_assets._nifti_stem
   nvitk.db.local_nifti_assets._normalize_slot_token
   nvitk.db.local_nifti_assets._classify_4dflow_path
   nvitk.db.local_nifti_assets._tof_slot_name
   nvitk.db.local_nifti_assets._iter_niftis_under
   nvitk.db.local_nifti_assets.register_nifti_tree
   nvitk.db.local_nifti_assets.upsert_nifti_assets
   nvitk.db.local_nifti_assets._cli_decorator
   nvitk.db.local_nifti_assets.main


Module Contents
---------------

.. py:data:: click
   :value: None


.. py:function:: _is_nifti_path(path: pathlib.Path) -> bool

   True if *path* has a ``.nii`` or ``.nii.gz`` extension.


.. py:function:: _nifti_stem(filename: str) -> str

   Filename without ``.nii`` / ``.nii.gz`` suffix.


.. py:function:: _normalize_slot_token(text: str) -> str

   Lower-case *text* and collapse runs of non-alphanumeric characters into single underscores, for
   building ``asset_slot`` names.


.. py:function:: _classify_4dflow_path(rel_posix: str, filename: str) -> tuple[str | None, str | None]

   Return ``(asset_slot, flow4d_kind)`` for a path under ``.../4DFlow/``.

   ``flow4d_kind`` is ``directional``, ``derived_global``, or ``None`` if unclassified.
   ``asset_slot`` is ``None`` when the layout does not match a known pattern.


.. py:function:: _tof_slot_name(stem: str, *, multi: bool) -> str

   ``"tof"`` when there's a single TOF NIfTI per subject, else ``"tof_<normalized stem>"`` to keep
   multiple TOF files distinct.


.. py:function:: _iter_niftis_under(root: pathlib.Path) -> Iterable[pathlib.Path]

   Yield every NIfTI file found anywhere under *root*, sorted; nothing if *root* isn't a directory.


.. py:function:: register_nifti_tree(nifti_root: str | pathlib.Path, *, source: str = 'local_nifti', pipeline_id: str | None = None, pipeline_name: str | None = None, source_batch_id: str | None = None) -> pandas.DataFrame

   Build ``assets`` rows for TOF and 4DFlow NIfTI trees under ``nifti_root``.


.. py:function:: upsert_nifti_assets(repo: nvitk.db.repo.DataRepo, nifti_root: str | pathlib.Path, *, source: str = 'local_nifti', pipeline_id: str | None = None, pipeline_name: str | None = None, source_batch_id: str | None = None, dry_run: bool = False, build_sqlite_index: bool = False) -> pandas.DataFrame

   Compute asset rows and upsert into ``repo`` unless ``dry_run``.


.. py:function:: _cli_decorator(*args: Any, **kwargs: Any)

   No-op stand-in for ``click.command``/``click.option`` when ``click`` isn't installed.


.. py:data:: _click_command

.. py:data:: _click_option

.. py:function:: main(dataset_root: pathlib.Path, nifti_root: pathlib.Path, source: str, pipeline_id: str | None, pipeline_name: str | None, source_batch_id: str | None, dry_run: bool, build_sqlite_index: bool) -> None

   CLI entry point: scaffold/open the dataset at ``dataset_root`` and register the reorganized
   NIfTI tree at ``nifti_root`` into the ``assets`` table.


