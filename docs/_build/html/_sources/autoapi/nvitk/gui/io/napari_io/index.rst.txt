nvitk.gui.io.napari_io
======================

.. py:module:: nvitk.gui.io.napari_io

.. autoapi-nested-parse::

   Load images into Napari via :mod:`nvitk.io` (NIfTI, DICOM, TIFF, MHA, ND2, …).



Attributes
----------

.. autoapisummary::

   nvitk.gui.io.napari_io._NVITK_OPEN_SUFFIXES
   nvitk.gui.io.napari_io.LayerData
   nvitk.gui.io.napari_io.ReaderFunc


Functions
---------

.. autoapisummary::

   nvitk.gui.io.napari_io._normalize_paths
   nvitk.gui.io.napari_io._nvitk_can_open
   nvitk.gui.io.napari_io._resolution_for_axis
   nvitk.gui.io.napari_io._napari_scale
   nvitk.gui.io.napari_io._napari_affine
   nvitk.gui.io.napari_io._nvitk_layer_metadata
   nvitk.gui.io.napari_io._axis_labels_for_image
   nvitk.gui.io.napari_io._prepare_layer_tuple
   nvitk.gui.io.napari_io._read_layer_data
   nvitk.gui.io.napari_io.read_paths
   nvitk.gui.io.napari_io._add_image_to_viewer
   nvitk.gui.io.napari_io.open_paths_with_nvitk
   nvitk.gui.io.napari_io._notify_error
   nvitk.gui.io.napari_io._is_nvitk_layer
   nvitk.gui.io.napari_io._layer_from_list_event
   nvitk.gui.io.napari_io._on_nvitk_layer_inserted
   nvitk.gui.io.napari_io._on_active_layer_sync_dims
   nvitk.gui.io.napari_io.install_nvitk_layer_hooks
   nvitk.gui.io.napari_io.install_nvitk_io


Module Contents
---------------

.. py:data:: _NVITK_OPEN_SUFFIXES

.. py:data:: LayerData

.. py:data:: ReaderFunc

.. py:function:: _normalize_paths(path: str | pathlib.Path | Sequence[str | pathlib.Path]) -> list[pathlib.Path]

   Coerce a single path or a sequence of paths into a list of :class:`Path` objects.


.. py:function:: _nvitk_can_open(path: pathlib.Path) -> bool

   True if *path* is a directory (assumed DICOM series) or a file nvitk's readers recognize by
   extension or content sniffing.


.. py:function:: _resolution_for_axis(md: dict[str, Any], axis_char: str) -> float | None

   Voxel/frame resolution for *axis_char* (``X``/``Y``/``Z``/``T``/``C``) from image metadata
   *md*, or ``None`` if not recorded.


.. py:function:: _napari_scale(img: nvitk.types.Image, ndim: int) -> tuple[float, Ellipsis] | None

   Per-array-axis scale aligned with ``img.axes`` (e.g. XYZT → x,y,z,t).


.. py:function:: _napari_affine(img: nvitk.types.Image) -> numpy.ndarray | None

   Raw 4x4 voxel-to-world matrix from the file (before display reorientation).


.. py:function:: _nvitk_layer_metadata(img: nvitk.types.Image, path: pathlib.Path, *, affine_source: numpy.ndarray | None) -> dict[str, Any]

   Napari ``metadata`` dict (nested nvitk fields only — no invalid layer kwargs).


.. py:function:: _axis_labels_for_image(img: nvitk.types.Image, ndim: int) -> tuple[str, Ellipsis]

   *img*'s axis labels if they match *ndim*, else the default NIfTI axis order for *ndim*.


.. py:function:: _prepare_layer_tuple(img: nvitk.types.Image, path: pathlib.Path) -> LayerData

   Build a napari-plugin-style ``(data, layer_kwargs, layer_type)`` tuple for *img*, reorienting
   the array for display and computing scale/affine, axis labels, and nvitk metadata from *path*.


.. py:function:: _read_layer_data(path: str) -> list[LayerData] | None

   Read one path into Napari layer tuples.


.. py:function:: read_paths(path: str | list[str]) -> ReaderFunc | list[LayerData] | None

   Napari npe2 reader command.

   When called with a single path string (npe2 command exec), returns a reader
   function. When that function is invoked, returns layer data tuples.


.. py:function:: _add_image_to_viewer(viewer: Any, img: nvitk.types.Image, path: pathlib.Path) -> Any

   Add *img* to *viewer* as an Image layer using :func:`_prepare_layer_tuple`'s display kwargs,
   then apply nvitk's viewer/dims configuration for the new layer.


.. py:function:: open_paths_with_nvitk(viewer: Any, paths: str | pathlib.Path | Sequence[str | pathlib.Path], *, stack=False, force_type: str | None = None) -> list[Any]

   Open one or more paths with nvitk.io and add Napari layers.

   *force_type* is forwarded to :func:`~nvitk.io.imread` (e.g. ``"nifti"`` when
   opening a directory that must not be treated as DICOM).


.. py:function:: _notify_error(message: str) -> None

   Show *message* via Napari's error notification, falling back to printing it if Napari's UI
   isn't available.


.. py:function:: _is_nvitk_layer(layer: Any) -> bool

   True if *layer* was loaded through nvitk's reader (carries an ``nvitk_metadata`` entry).


.. py:function:: _layer_from_list_event(event: Any) -> Any | None

   Layer instance from Napari ``inserted`` / legacy ``added`` events.


.. py:function:: _on_nvitk_layer_inserted(viewer: Any, event: Any) -> None

   Configure viewer dims/orientation for a newly inserted layer: repairs the time-dim range for
   overlays and 4D+ layers, and applies nvitk's display setup for 4D+ or nvitk-sourced layers.


.. py:function:: _on_active_layer_sync_dims(viewer: Any, _event: Any) -> None

   Re-apply 4D dims when selecting a 4D layer (3D oblique affines can pollute viewer.dims).


.. py:function:: install_nvitk_layer_hooks(viewer: Any) -> None

   Configure dims when layers are added by the nvitk-io reader (not only Qt open).


.. py:function:: install_nvitk_io(viewer: Any) -> None

   Hook Qt open/drop paths to nvitk.io.

   Napari's Viewer model is Pydantic and does not allow patching ``open``;
   we wrap ``QtViewer._qt_open`` instead.


