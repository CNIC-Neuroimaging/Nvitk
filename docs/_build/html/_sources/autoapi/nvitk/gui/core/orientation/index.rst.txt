nvitk.gui.core.orientation
==========================

.. py:module:: nvitk.gui.core.orientation

.. autoapi-nested-parse::

   Napari display: file affine + axial viewing along Superior axis.



Functions
---------

.. autoapisummary::

   nvitk.gui.core.orientation.suppress_nonorthogonal_slice_warning
   nvitk.gui.core.orientation.superior_voxel_axis
   nvitk.gui.core.orientation.axial_dim_order
   nvitk.gui.core.orientation.napari_dim_order_3d
   nvitk.gui.core.orientation._axes_string_from_layer
   nvitk.gui.core.orientation.napari_dim_order
   nvitk.gui.core.orientation._resolution_from_metadata
   nvitk.gui.core.orientation.napari_scale_for_display
   nvitk.gui.core.orientation.napari_affine_for_display
   nvitk.gui.core.orientation.prepare_for_napari
   nvitk.gui.core.orientation._lr_voxel_axis_for_radiological
   nvitk.gui.core.orientation._apply_voxel_axis_flip
   nvitk.gui.core.orientation.reorient_layer_for_view
   nvitk.gui.core.orientation._layer_display_scale
   nvitk.gui.core.orientation._metadata_for_layer
   nvitk.gui.core.orientation.ensure_4d_scale_only_layer
   nvitk.gui.core.orientation._synchronize_4d_dims
   nvitk.gui.core.orientation.configure_viewer_for_layer


Module Contents
---------------

.. py:function:: suppress_nonorthogonal_slice_warning() -> Iterator[None]

   Hide Napari's oblique-acquisition slice warning (display is still approximate).


.. py:function:: superior_voxel_axis(affine: numpy.ndarray | None, ndim: int = 3) -> int

   Array axis index whose +direction is closest to patient Superior.


.. py:function:: axial_dim_order(affine: numpy.ndarray | None, ndim: int = 3) -> tuple[int, Ellipsis]

   Dims order with Superior first so 2D mode steps axially through the volume.


.. py:function:: napari_dim_order_3d(affine: numpy.ndarray | None, ndim: int = 3) -> tuple[int, Ellipsis]

   Dims order for 3D axial viewing in Napari (matches in-plane layout after Ctrl+T).

   ``axial_dim_order`` alone yields ``(S, A, R)``; Napari's default display matches
   ``(S, R, A)`` for typical RAS NIfTI — the same result as ``dims.transpose()`` once,
   without calling transpose on load.


.. py:function:: _axes_string_from_layer(layer: Any) -> str | None

   Recover *layer*'s axis-order string (e.g. ``"XYZT"``) from its ``axis_labels`` or nvitk
   metadata; ``None`` if unavailable.


.. py:function:: napari_dim_order(axes: str | None, affine: numpy.ndarray | None, ndim: int) -> tuple[int, Ellipsis]

   Napari ``dims.order``: non-displayed axes first (time), then spatial axes to render.

   For ``XYZT`` data this yields ``(T, X, Y, Z)`` so the time slider drives axis ``T`` and
   the 3D view uses array ``X``, ``Y``, ``Z`` (no superior-first shuffle on 4D+).


.. py:function:: _resolution_from_metadata(metadata: dict[str, Any] | None, axis_char: str) -> float | None

   Voxel/frame resolution for *axis_char* (``X``/``Y``/``Z``/``T``/``C``) from nvitk *metadata*,
   or ``None`` if not recorded.


.. py:function:: napari_scale_for_display(shape: tuple[int, Ellipsis], axes: str | None, metadata: dict[str, Any] | None = None) -> tuple[float, Ellipsis]

   Diagonal voxel spacing per array axis (used for 4D+ instead of affine).


.. py:function:: napari_affine_for_display(affine: numpy.ndarray | None, shape: tuple[int, Ellipsis], axes: str | None, metadata: dict[str, Any] | None = None) -> numpy.ndarray | None

   Affine for Napari display.

   For 4D+ volumes, decouple the temporal axis from spatial world coordinates so the
   time slider spans voxel indices (e.g. 15 phases), not a distorted world extent.


.. py:function:: prepare_for_napari(data: numpy.ndarray, affine: numpy.ndarray | None, *, axes: str | None = None, metadata: dict[str, Any] | None = None, radiological: bool = True) -> tuple[numpy.ndarray, numpy.ndarray | None, tuple[float, Ellipsis] | None]

   Prepare layer display transforms.

   4D+ uses ``scale`` only (no affine). Oblique 4DFlow affines couple spatial axes into
   the temporal world axis and Napari then reports ~60 bogus time steps instead of 15.
   The file affine is preserved in metadata as ``affine_source`` for export.


.. py:function:: _lr_voxel_axis_for_radiological(affine: numpy.ndarray | None, ndim: int) -> int | None

   Voxel axis to flip for radiological in-plane L/R (patient R on screen left).


.. py:function:: _apply_voxel_axis_flip(layer: Any, axis: int) -> None

   Mirror one voxel axis in the viewer by negating that column of the affine.


.. py:function:: reorient_layer_for_view(layer: Any, target: str) -> tuple[str, str | None]

   Change Napari display to *target* axis codes (e.g. ``LAS``).

   Sign-only changes (R↔L, A↔P, S↔I) mirror the viewer via affine flips without
   rewriting voxel data. Permutations reorder ``layer.data`` and rebuild a display
   affine so the on-screen layout matches *target*.

   Unlike :meth:`nvitk.types.Image.orient_to`, this does not preserve world-space
   coordinates; the goal is a visible left/right (or axis) flip in the viewer.


.. py:function:: _layer_display_scale(layer: Any, ndim: int) -> tuple[float, Ellipsis]

   *layer*'s per-axis display scale for the first *ndim* axes, or all-ones if unset/too short.


.. py:function:: _metadata_for_layer(layer: Any) -> dict[str, Any]

   *layer*'s nvitk metadata dict, unwrapping the ``nvitk_metadata`` sub-key when present.


.. py:function:: ensure_4d_scale_only_layer(layer: Any) -> None

   Force 4D+ layers to diagonal scale (no oblique file affine).

   Oblique 4DFlow affines break the time slider and cause out-of-bounds indices.


.. py:function:: _synchronize_4d_dims(viewer: Any, layer: Any, *, axes_str: str | None, shape: tuple[int, Ellipsis]) -> None

   Force Napari dims range/point from array shape (15 phases, not ~62).


.. py:function:: configure_viewer_for_layer(viewer: Any, layer: Any, *, radiological: bool = False, configure_dims: bool | None = None) -> None

   Axial-friendly dims for 3D; preserve 4D+ volumes (time/other axes untouched).

   Radiological L/R affine flips are off by default so raw images align with label
   masks opened from the same NIfTI grid (file affine, no extra X mirror).

   Set ``configure_dims=False`` when adding further layers (e.g. tool outputs) so the
   user's ndisplay, axis order, transpose, and camera are left unchanged.


