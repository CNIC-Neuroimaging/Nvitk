nvitk.pipes.pesa_fat.qc.expected_ranges
=======================================

.. py:module:: nvitk.pipes.pesa_fat.qc.expected_ranges

.. autoapi-nested-parse::

   Expected value thresholds for QC table highlighting.

   Keys match spreadsheet column names from stage-3 ``column_order()`` (excluding ``pesa_id``).
   Values are ``(floor, warn_high, bad_high)``:

   - red if value < floor (when floor finite)
   - orange if value > warn_high (when warn_high finite) AND value <= bad_high (when bad_high finite)
   - red if value > bad_high (when bad_high finite)

   Use ``None`` or ``nan`` to disable any threshold.



Attributes
----------

.. autoapisummary::

   nvitk.pipes.pesa_fat.qc.expected_ranges.EXPECTED_RANGES_CTPET
   nvitk.pipes.pesa_fat.qc.expected_ranges.EXPECTED_RANGES_DIXON


Functions
---------

.. autoapisummary::

   nvitk.pipes.pesa_fat.qc.expected_ranges.cell_level


Module Contents
---------------

.. py:data:: EXPECTED_RANGES_CTPET
   :type:  dict[str, Threshold]

.. py:data:: EXPECTED_RANGES_DIXON
   :type:  dict[str, Threshold]

.. py:function:: cell_level(column: str, value: float | int | None, ranges: dict[str, Threshold]) -> str | None

   Return 'warn' | 'bad' | None for the given cell.


